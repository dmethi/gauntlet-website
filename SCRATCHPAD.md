# Scratchpad

> Agent memory between runs. WIP only — settled facts go to module cards.

## Active

- **Seeding backlog** (factory→diffusion migration, 2026-06-25).
  `docs/AGENTS.md` + synced `ENGINEERING_PRINCIPLES.md` present; module cards /
  ROADMAP / test sweep still to seed.

## Seeding backlog

1. **Finish diffusion scaffold** — module cards `docs/modules/*.md`
   (invariants/patterns/vocabulary) and `ROADMAP.md` (direction + decision
   register) if absent; confirm `docs/AGENTS.md` reflects the diffusion work
   mode. Pattern: menshealth/soundwave.
2. **Testing seed/audit** (`../shared-agent-config/skills/testing.md`) — sweep
   two angles (seams + doc/card invariants), enforce each on the type→lint→test
   ladder, flag untestable. Must-cover anchors on hard constraints (type safety,
   leagues processed separately until presentation — see AGENTS.md).

## Blocked

- None

## Next

1. Capture league-separation + type invariants in module cards.
2. Testing seed (item 2).

## Session notes

- 2026-06-25 — Scratchpad created as a seeding breadcrumb during the
  factory→diffusion migration.
