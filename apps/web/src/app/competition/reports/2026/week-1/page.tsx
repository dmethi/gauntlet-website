import {
  WEEK_ONE_RECAP_METADATA,
  WeekOneRecapPage,
} from '@/features/weekly-recap/week-one-report-page';

export const metadata = WEEK_ONE_RECAP_METADATA;

export const dynamic = 'force-dynamic';

export default WeekOneRecapPage;
