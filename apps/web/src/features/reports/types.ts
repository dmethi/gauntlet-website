/**
 * Reports Types
 *
 * Types for report generation and narrative analysis.
 */

/**
 * Placeholder type for reports feature
 * Add report-specific types as needed
 */
export interface ReportPlaceholder {
  id: string;
  name: string;
}
